import React from "react";
import { Layout } from "antd";
const { Header } = Layout;

function Head() {
  return (
    <Header style={{ "text-align": "center" }}>
      <h1 style={{ margin: 2, color: "white" }}>Clementechs</h1>
    </Header>
  );
}

export default Head;
